# DBConnector 
DBConnector is a API in development that will allow easy connection configuration to any Database given a JDBC driver is avaliable. Many existing databases are pre-configured, new databases can be added by extending the dblist.properties file. 

A simple SWT GUI will also be provided to easily understand how the API works and needs to be implemented. 
